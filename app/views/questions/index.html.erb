<h2>Questions</h2>

<%= link_to "Add a Question", new_question_path %>
<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>&nbsp;</th>
    </tr>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.id %></td>
        <td><%= question.title %></td>
        <td><%= link_to 'Details', question_path(question) %> | <%= link_to 'Edit', edit_question_path(question) %> | <%= link_to 'Delete', question_path(question), method: :delete  %></td>
      </tr>
    <% end %>
  </thead>
</table