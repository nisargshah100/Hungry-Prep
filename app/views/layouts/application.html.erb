<!DOCTYPE html>
<html>
<head>
  <title>HungryPrep</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= javascript_include_tag 'jquery', 'rails.validations' %>
  <%= csrf_meta_tags %>
</head>
<body>
<header class="global-header">
  <div class="header-inner container">
    <h1 class="logo"><%= link_to "Hungry Academy", "/" %></h1>

    <div class="account columns six">
      <% if current_user %>
        <span class="user"><%= current_user.name %></span>
        <span class="last-saved">Application saved <span id="time"></span></span>
        <span class="user-actions"><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></span>
      <% end %>
    </div>
  </div>
</header>
<div class="cobrand container">
  powered by <span class="logo logo-ls">LivingSocial</span> &amp; <span class="logo logo-jumpstart">Jumpstart Labs</span>
</div>
<div class="page-wrap">
  <div class="page-content">

    <%= yield %>
  </div>
</div>
</body>
</html>
